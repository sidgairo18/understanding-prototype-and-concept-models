"""Shared, paper-agnostic utilities reused across every POC.

Import the pieces you need rather than reimplementing them inside a POC folder:

    from common.data.cub import CUB200, build_transforms
    from common.backbones import build_backbone
    from common import viz
"""
